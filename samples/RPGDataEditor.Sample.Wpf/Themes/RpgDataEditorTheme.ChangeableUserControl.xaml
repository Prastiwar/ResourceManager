<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:RPGDataEditor.Sample.Wpf.Controls">
    <Style TargetType="{x:Type controls:RequirementView}">
        <Setter Property="Template"
                Value="{StaticResource ChangeableUserControlTemplate}" />
    </Style>
    <Style TargetType="{x:Type controls:QuestTaskView}">
        <Setter Property="Template"
                Value="{StaticResource ChangeableUserControlTemplate}" />
    </Style>
    <Style TargetType="{x:Type controls:DialogueOptionView}">
        <Setter Property="Template"
                Value="{StaticResource ChangeableUserControlTemplate}" />
    </Style>
    <Style TargetType="{x:Type controls:NpcJobView}">
        <Setter Property="Template"
                Value="{StaticResource ChangeableUserControlTemplate}" />
    </Style>
</ResourceDictionary>