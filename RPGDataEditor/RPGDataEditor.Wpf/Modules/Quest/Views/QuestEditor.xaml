<UserControl x:Class="RPGDataEditor.Wpf.Quest.Views.QuestEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:behaviors="clr-namespace:RPGDataEditor.Wpf.Behaviors"
             xmlns:local="clr-namespace:RPGDataEditor.Wpf"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:questViews="clr-namespace:RPGDataEditor.Wpf.Quest.Views"
             xmlns:controls="clr-namespace:RPGDataEditor.Wpf.Controls"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="#424242"
             FontFamily="{DynamicResource MaterialDesignFont}"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto">
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Style.Setters>
                <Setter Property="MinHeight"
                        Value="512" />
                <Setter Property="MinWidth"
                        Value="800" />
            </Style.Setters>
        </Style>
    </prism:Dialog.WindowStyle>
    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ScrollViewer>
            <StackPanel Margin="10">
                <TextBox DataContext="{Binding Model}"
                         Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Margin="5"
                         materialDesign:HintAssist.Hint="Title"
                         Style="{DynamicResource MaterialDesignFloatingHintTextBox}">
                    <i:Interaction.Behaviors>
                        <behaviors:CatchValidationBehavior />
                    </i:Interaction.Behaviors>
                </TextBox>
                <TextBox DataContext="{Binding Model}"
                         Text="{Binding Message, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Margin="5"
                         materialDesign:HintAssist.Hint="Message"
                         Style="{DynamicResource MaterialDesignFloatingHintTextBox}">
                    <i:Interaction.Behaviors>
                        <behaviors:CatchValidationBehavior />
                    </i:Interaction.Behaviors>
                </TextBox>
                <materialDesign:Card HorizontalAlignment="Stretch"
                                     HorizontalContentAlignment="Center"
                                     DataContext="{Binding Model}"
                                     Margin="5"
                                     Padding="5">
                    <StackPanel>
                        <TextBlock Text="Choose completion task"
                                   Margin="5" />
                        <controls:QuestTaskView DataContext="{Binding CompletionTask, Mode=TwoWay}"
                                                TypeChange="CompletionQuestTaskView_TypeChange"
                                                Grid.Column="1"
                                                HorizontalAlignment="Stretch"
                                                HorizontalContentAlignment="Stretch" />
                    </StackPanel>
                </materialDesign:Card>
                <materialDesign:Card HorizontalAlignment="Stretch"
                                     HorizontalContentAlignment="Center"
                                     Margin="5"
                                     Padding="5">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <controls:ExpandButton ExpandableElement="{x:Reference TasksListView}" />
                            <Button Command="{Binding AddTaskCommand}"
                                    Style="{DynamicResource AddFlatButtonStyle}" />
                            <TextBlock Text="{Binding Count, StringFormat='({0}) Tasks'}"
                                       DataContext="{Binding ItemsSource, ElementName=TasksListView}"
                                       Margin="5" />
                        </StackPanel>
                        <ListView x:Name="TasksListView"
                                  DataContext="{Binding Model}"
                                  ItemsSource="{Binding Tasks}"
                                  HorizontalAlignment="Stretch"
                                  AlternationCount="{StaticResource MaxIntValue}"
                                  HorizontalContentAlignment="Stretch">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <materialDesign:Card HorizontalAlignment="Stretch"
                                                         HorizontalContentAlignment="Center"
                                                         Padding="5">
                                        <Grid HorizontalAlignment="Stretch">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Button Command="{Binding DataContext.RemoveTaskCommand, Source={x:Reference MainGrid}}"
                                                    CommandParameter="{Binding .}"
                                                    VerticalAlignment="Top"
                                                    Margin="0,12,0,0"
                                                    Style="{StaticResource DeleteFlatButtonStyle}" />
                                            <controls:QuestTaskView DataContext="{Binding .}"
                                                                    TypeChange="QuestTaskViewList_TypeChange"
                                                                    Grid.Column="1"
                                                                    local:AttachProperties.ValidablePathFormat="Tasks[{0}]"
                                                                    local:AttachProperties.ValidableObject="{Binding DataContext, ElementName=TasksListView}"
                                                                    HorizontalAlignment="Stretch"
                                                                    HorizontalContentAlignment="Stretch">
                                                <local:AttachProperties.ValidablePathValues>
                                                    <MultiBinding Converter="{StaticResource BindingListConverter}">
                                                        <Binding Path="(ItemsControl.AlternationIndex)"
                                                                 RelativeSource="{RelativeSource AncestorType={x:Type ListBoxItem}}" />
                                                    </MultiBinding>
                                                </local:AttachProperties.ValidablePathValues>
                                            </controls:QuestTaskView>
                                        </Grid>
                                    </materialDesign:Card>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </materialDesign:Card>
                <materialDesign:Card HorizontalAlignment="Stretch"
                                     HorizontalContentAlignment="Center"
                                     Margin="5"
                                     Padding="5">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <controls:ExpandButton ExpandableElement="{x:Reference RequirementsListView}" />
                            <Button Command="{Binding AddRequirementCommand}"
                                    Style="{DynamicResource AddFlatButtonStyle}" />
                            <TextBlock Text="{Binding ItemsSource.Count, ElementName=RequirementsListView, StringFormat='({0}) Requirements'}"
                                       Margin="5" />
                        </StackPanel>
                        <ListView x:Name="RequirementsListView"
                                  DataContext="{Binding Model}"
                                  ItemsSource="{Binding Requirements}"
                                  AlternationCount="{StaticResource MaxIntValue}"
                                  HorizontalAlignment="Stretch"
                                  HorizontalContentAlignment="Stretch">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <materialDesign:Card HorizontalAlignment="Stretch"
                                                         HorizontalContentAlignment="Center"
                                                         Padding="5">
                                        <Grid HorizontalAlignment="Stretch">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Button Command="{Binding DataContext.RemoveRequirementCommand, Source={x:Reference MainGrid}}"
                                                    CommandParameter="{Binding .}"
                                                    VerticalAlignment="Top"
                                                    Margin="0,12,0,0"
                                                    Style="{StaticResource DeleteFlatButtonStyle}" />
                                            <controls:RequirementView DataContext="{Binding .}"
                                                                      Grid.Column="1"
                                                                      TypeChange="RequirementView_TypeChange"
                                                                      local:AttachProperties.ValidablePathFormat="Requirements[{0}]"
                                                                      local:AttachProperties.ValidableObject="{Binding DataContext, ElementName=RequirementsListView}"
                                                                      HorizontalAlignment="Stretch"
                                                                      HorizontalContentAlignment="Stretch">
                                                <local:AttachProperties.ValidablePathValues>
                                                    <MultiBinding Converter="{StaticResource BindingListConverter}">
                                                        <Binding Path="(ItemsControl.AlternationIndex)"
                                                                 RelativeSource="{RelativeSource AncestorType={x:Type ListBoxItem}}" />
                                                    </MultiBinding>
                                                </local:AttachProperties.ValidablePathValues>
                                            </controls:RequirementView>
                                        </Grid>
                                    </materialDesign:Card>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>
        </ScrollViewer>
        <ContentPresenter Grid.Row="1"
                          ContentTemplate="{StaticResource SaveCancelButtonsTemplate}" />
    </Grid>
</UserControl>
