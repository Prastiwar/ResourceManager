<UserControl x:Class="RPGDataEditor.Wpf.Quest.Views.QuestEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:behaviors="clr-namespace:RPGDataEditor.Wpf.Behaviors"
             xmlns:local="clr-namespace:RPGDataEditor.Wpf"
             xmlns:controls="clr-namespace:RPGDataEditor.Wpf.Controls"
             Style="{DynamicResource DefaultEditorControl}">
    <StackPanel Margin="10">
        <TextBox DataContext="{Binding Model}"
                 Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Margin="5"
                 materialDesign:HintAssist.Hint="Title"
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}">
            <i:Interaction.Behaviors>
                <behaviors:CatchValidationBehavior />
            </i:Interaction.Behaviors>
        </TextBox>
        <TextBox DataContext="{Binding Model}"
                 Text="{Binding Message, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Margin="5"
                 materialDesign:HintAssist.Hint="Message"
                 Style="{DynamicResource MaterialDesignFloatingHintTextBox}">
            <i:Interaction.Behaviors>
                <behaviors:CatchValidationBehavior />
            </i:Interaction.Behaviors>
        </TextBox>
        <materialDesign:Card HorizontalAlignment="Stretch"
                             HorizontalContentAlignment="Center"
                             DataContext="{Binding Model}"
                             Margin="5"
                             Padding="5">
            <StackPanel>
                <TextBlock Text="Choose completion task"
                           Margin="5" />
                <controls:QuestTaskView DataContext="{Binding CompletionTask}"
                                        TypeChange="CompletionQuestTaskView_TypeChange"
                                        Grid.Column="1"
                                        HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Stretch" />
            </StackPanel>
        </materialDesign:Card>
        <controls:ListDataCard x:Name="TasksListDataCard"
                               AddItemCommand="{Binding DataContext.AddTaskCommand, RelativeSource={RelativeSource AncestorType=StackPanel}}"
                               DataContext="{Binding Model}"
                               HeaderText="{Binding Tasks.Count, StringFormat='({0}) Tasks'}"
                               ItemsSource="{Binding Tasks}">
            <DataTemplate>
                <controls:QuestTaskView DataContext="{Binding .}"
                                        TypeChange="QuestTaskViewList_TypeChange"
                                        Grid.Column="1"
                                        local:AttachProperties.ValidablePathFormat="Tasks[{0}]"
                                        local:AttachProperties.ValidableObject="{Binding DataContext, ElementName=TasksListDataCard}"
                                        HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Stretch">
                    <local:AttachProperties.ValidablePathValues>
                        <MultiBinding Converter="{StaticResource BindingListConverter}">
                            <Binding Path="(ItemsControl.AlternationIndex)"
                                     RelativeSource="{RelativeSource AncestorType={x:Type ListBoxItem}}" />
                        </MultiBinding>
                    </local:AttachProperties.ValidablePathValues>
                </controls:QuestTaskView>
            </DataTemplate>
        </controls:ListDataCard>
        <controls:RequirementsListCard x:Name="RequirementsCard"
                                       DataContext="{Binding Model}"
                                       Requirements="{Binding Requirements}"
                                       local:AttachProperties.ValidablePathFormat="Requirements[{0}]"
                                       local:AttachProperties.ValidableObject="{Binding DataContext, ElementName=RequirementsCard}">
            <controls:RequirementsListCard.ValidablePathValuesBindings>
                <local:MultiBindingValue>
                    <Binding Path="(ItemsControl.AlternationIndex)"
                             RelativeSource="{RelativeSource AncestorType={x:Type ListBoxItem}}" />
                </local:MultiBindingValue>
            </controls:RequirementsListCard.ValidablePathValuesBindings>
        </controls:RequirementsListCard>
    </StackPanel>
</UserControl>
